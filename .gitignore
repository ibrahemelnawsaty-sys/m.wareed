/.phpunit.cache
/node_modules
# public/build is committed on purpose: Hostinger shared hosting has no Node,
# so the compiled Vite assets must ship with the repo (see docs/DEPLOY.md).
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Local dev database
/database/*.sqlite
/database/*.sqlite-journal
